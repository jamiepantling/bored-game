<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/../stylesheets/style.css" />
  </head>
  <body class="bg-indigo-50">
    <div
      class="py-3 flex justify-between align-middle items-center  bg-[#27272e] text-white h-28"
    >
      <div class="ml-3">
        <% if (user) { %>
        <a href="/games"
          ><button
            class="mx-2 my-2 bg-indigo-800 transition duration-150 ease-in-out hover:bg-indigo-600 rounded text-white px-6 py-2 font-bold text-xs sm:text-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-600"
          >
            All games
          </button></a
        ><a href="/games/new"
          ><button
            class="mx-2 my-2 bg-indigo-800 transition duration-150 ease-in-out hover:bg-indigo-600 rounded text-white px-6 py-2 text-xs sm:text-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-600 font-bold"
          >
            Add game
          </button></a
        >
        <%}%>
      </div>
      <div
        class="text-center sm:text-5xl text-3xl font-title font-bold py-4"
      >
        <%=title%>
      </div>

      <div class="flex justify-around align-middle mr-5 ">
        <% if (user) { %>
        
        <div class="ml-5 dropdown">
          <img
            referrerpolicy="no-referrer"
            class="rounded-full h-16 border border-gray-500"
            src="<%=user.picture%>"
          />
          <div class="dropdown-option flex align-middle justify-center ml-5 my-auto">
            <a class="sm:text-xl text-md font-bold" href="/logout">Log Out</a>
          </div>
        </div>

        <% } else { %>
        <a href="/auth/google">Login with Google</a>
        <% } %>
      </div>
    </div>
  </body>
</html>
